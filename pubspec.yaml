name: testapp
description: Flutter Test App Infonet Global Tech

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^4.0.4
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  provider: ^6.0.5
  path_provider: ^2.1.1
  carousel_slider: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true